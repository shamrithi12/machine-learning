import pandas as pd 
import numpy as np 
importseaborn assns
import matplotlib.pyplot as plt
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA
from sklearn.model_selection import train_test_split



df=pd.read_csv(r'D:/pg/II SEM/ML/MLdataset/MSC DA -ML Datasets (2024 -25 
Batch)/bank- full.csv',delimiter=';')
print(df.head())


df.describe()



df.shape


sns.countplot(x='y',data=df,label ="Count")
 plt.xlabel(“count”) 
 plt.show()


# Preprocessing: Handle categorical columns using One-Hot Encoding
df_encoded=pd.get_dummies(df,drop_first=True)
# Check for missing values (if any)
print(f"Missing values: \n{df_encoded.isnull().sum()}")


plt.figure(figsize=(15,5))
sns.countplot(x ="job" , data = df , label = "Count") 
plt.show()

sns.countplot(x ="marital", data =df ,hue ="y") 
plt.show()


plt.figure(figsize=(15,5)) 
sns.countplot(x="education" , data = df , label = "y") 
plt.show()


sns.countplot(x ="marital", data =df ,hue ="y") 
plt.show()


plt.figure(figsize=(15,5)) 
sns.countplot(x="education" , data = df , label = "y") 
plt.show()


# Normalize the data (standardization) to make the PCA more effective
scalar =StandardScaler()
df_scaled = scalar.fit_transform(df_encoded)
# Apply PCA to reduce dimensions
pca = PCA(n_components=2)
df_pca = pca.fit_transform(df_scaled)
# Create a DataFrame to hold the PCA results
df_pca_df = pd.DataFrame(df_pca, columns=['PCA1', 'PCA2'])
# Explained variance ratio for each component 
explained_variance = pca.explained_variance_ratio_ 
print(explained_variance)




# Visualize the explained variance
plt.figure(figsize=(8, 6))
plt.bar(range(1, len(explained_variance)+1), explained_variance, alpha=0.7, align='center') 
plt.xlabel('Principal Components')
plt.ylabel('Explained Variance Ratio') 
plt.title('Explained Variance by Principal Components') 
plt.show()


Visualize the data in the first two principal components (PCA1 vs PCA2)
plt.figure(figsize=(8, 6))
sns.scatterplot(x=df_pca_df['PCA1'], y=df_pca_df['PCA2'], alpha=0.7, s=60) plt.title('PCA: Bank 
Marketing Data')
plt.xlabel('Principal Component 1')
plt.ylabel('Principal Component 2') 
plt.show()




